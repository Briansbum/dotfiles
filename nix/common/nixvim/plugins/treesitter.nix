# Treesitter configuration
{ ... }:
{
  plugins = {
    treesitter = {
      enable = true;
      indent.enable = true;
      highlight.enable = true;
      settings.auto_install = false;
    };
  };
}
